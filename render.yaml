services:
  - type: web
    name: kachra-ai
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --timeout 120"
    region: frankfurt
    branch: main
